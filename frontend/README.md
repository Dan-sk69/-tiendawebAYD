# Frontend

Estructura inicial preparada con React + Vite para consumir el backend.

## Configuracion

Copia `.env.example` a `.env` si necesitas cambiar la URL de la API:

```text
VITE_API_URL=http://localhost:8080/api
```

## Comandos

```bash
npm install
npm run dev
```
